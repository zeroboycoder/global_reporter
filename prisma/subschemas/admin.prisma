model Admin {
  id          Int      @id @default(autoincrement())
  adminId     String
  password    String
  name        String
  ipAddresses String[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([adminId, name])
}
